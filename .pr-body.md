fix: MockClaw core bug fixes + graceful degradation

## Fixes

### Critical Bug Fixes
- generator.py: Fixed fallback code always returning 500 error
  - Now correctly uses HAR-recorded status codes (200, 400, 404, etc.)
  - Supports multiple responses per endpoint (sample_responses)
  - Skips builtin endpoints to avoid duplicates

### Feature Improvements
- Docker deployment is now optional (graceful degradation)
- ChaosBreaker import failure is handled gracefully
- validate() uses ASGI transport instead of HTTP server
- No longer crashes when Docker/services unavailable

### Code Quality
- ruff check: 20 errors -> 0 errors
- Removed all bare except clauses
- Added complete type hints and docstrings
- Tests: 7/8 -> 8/8 passing

## Test Results

```
pytest: 8 passed, 8 warnings
ruff: All checks passed!
Immortal Agent: Iteration completed: OK
```

## Checklist

- [x] Core functionality works correctly
- [x] All tests passing
- [x] Code standards met
- [x] No critical bugs
- [x] Graceful degradation

Co-authored-by: QClaw Agent <agent@qclaw.ai>
