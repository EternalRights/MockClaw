# 🎯 MockClaw 新手日常开发指南

> 本指南专为测试开发新手设计，教你如何在日常开发中使用 MockClaw

---

## 📚 目录

1. [MockClaw 能帮你解决什么问题？](#mockclaw-能帮你解决什么问题)
2. [日常开发的 3 个典型场景](#日常开发的 3 个典型场景)
3. [快速上手：5 分钟体验](#快速上手5 分钟体验)
4. [场景 1：前端开发 - 后端接口还没好](#场景 1 前端开发---后端接口还没好)
5. [场景 2：测试开发 - 创建测试环境](#场景 2 测试开发---创建测试环境)
6. [场景 3：本地调试 - 模拟第三方 API](#场景 3 本地调试---模拟第三方-api)
7. [常用命令速查表](#常用命令速查表)
8. [常见问题解答](#常见问题解答)

---

## 🤔 MockClaw 能帮你解决什么问题？

### 痛点 1：后端接口没开发完，前端等着联调
**传统做法**：手写 Mock 数据，每个接口都要手动写  
**MockClaw 做法**：录制一次真实请求，自动生成所有 Mock 接口

### 痛点 2：测试环境不稳定，经常被破坏
**传统做法**：求爷爷告奶奶找运维恢复环境  
**MockClaw 做法**：本地启动 Mock 服务器，想怎么玩就怎么玩

### 痛点 3：第三方 API 有限流，调试成本高
**传统做法**：小心翼翼，生怕触发限流  
**MockClaw 做法**：用 Mock 数据随便测试，不花一分钱

### 痛点 4：接口文档过时，参数全靠猜
**传统做法**：抓着后端小哥问个不停  
**MockClaw 做法**：自动生成最新的 API 文档

---

## 🎬 日常开发的 3 个典型场景

### 场景 1：前端开发
> 你是前端开发，后端接口还没好，但你要开始开发页面

**工作流程**：
1. 找后端小哥要一个测试环境的 HAR 文件（或自己录制）
2. 用 MockClaw 生成 Mock 服务器
3. 前端代码指向本地 Mock 服务器（http://localhost:8000）
4. 开心开发，不再依赖后端！

### 场景 2：测试开发
> 你是测试开发，要写自动化测试，但环境不稳定

**工作流程**：
1. 录制生产环境的真实流量（用户完整操作流程）
2. 生成 Mock 服务器
3. 在 Mock 服务器上运行测试
4. 测试稳定、可重复、不依赖外部系统！

### 场景 3：本地调试
> 你要调试调用第三方 API 的代码，但不想每次都发真实请求

**工作流程**：
1. 录制一次第三方 API 的真实调用
2. 生成 Mock 服务器
3. 修改 hosts 或代理，将第三方域名指向本地
4. 随意调试，不触发限流！

---

## 🚀 快速上手：5 分钟体验

### 步骤 1：启动服务（1 分钟）

```bash
# 方式 A：使用启动脚本（推荐）
start.bat

# 方式 B：手动启动
# 终端 1 - 后端
python src/brain.py

# 终端 2 - 前端
cd web
npm run dev
```

**验证启动成功**：
- 后端：访问 http://localhost:8000/health
- 前端：访问 http://localhost:3000

### 步骤 2：录制流量（2 分钟）

```bash
# 启动虚拟商店（模拟真实 API）
python tests/gauntlet/dummy_shop.py

# 录制用户会话
python scripts/gauntlet_recorder.py
```

这会生成 `tests/gauntlet/flow.har` 文件

### 步骤 3：生成 Mock（1 分钟）

**方式 A：使用前端界面**
1. 访问 http://localhost:3000
2. 拖入 `tests/gauntlet/flow.har` 到 Traffic 页面
3. 点击 "Generate All"

**方式 B：使用命令行**
```bash
mockclaw generate tests/gauntlet/flow.har --no-llm
```

### 步骤 4：启动 Mock 服务器（1 分钟）

```bash
# 启动 Mock 服务器
mockclaw serve generated_mocks --port 8000

# 访问 API 文档
# http://localhost:8000/docs
```

**完成！** 🎉 你现在有一个完整的 Mock API 服务器了！

---

## 📝 场景 1：前端开发 - 后端接口还没好

### 真实案例：开发一个电商网站的商品列表页

#### 步骤 1：获取后端接口的 HAR 文件

**方法 A：从测试环境录制**
1. 打开测试环境网站
2. 浏览器按 F12，切换到 Network 标签
3. 浏览商品列表、筛选、排序等操作
4. 右键 Network 标签 → "Save all as HAR with content"
5. 保存为 `backend_api.har`

**方法 B：使用 MockClaw 的虚拟商店**
```bash
# 启动虚拟商店
python tests/gauntlet/dummy_shop.py

# 访问并操作
# http://localhost:9000

# 录制流量
python scripts/gauntlet_recorder.py
```

#### 步骤 2：生成 Mock 服务器

```bash
# 使用智能回退模式（无需 LLM API Key）
mockclaw generate backend_api.har --no-llm

# 输出示例：
# 📦 Parsing HAR file: backend_api.har
# ✅ Found 8 endpoints
# 🤖 Generating mocks...
# ✅ Generated 8/8 endpoints
# 📂 Output directory: generated_mocks
```

#### 步骤 3：启动 Mock 服务器

```bash
# 启动在 8000 端口
mockclaw serve generated_mocks --port 8000
```

#### 步骤 4：修改前端代码

```javascript
// 原来的 API 地址
// const API_BASE = 'http://backend-server:8080/api'

// 改为本地 Mock 服务器
const API_BASE = 'http://localhost:8000'

// 你的前端代码无需其他修改！
fetch(`${API_BASE}/products`)
  .then(res => res.json())
  .then(data => {
    // 和真实后端返回的数据结构一样！
    console.log(data)
  })
```

#### 步骤 5：查看 API 文档

访问 http://localhost:8000/docs

你可以：
- 查看所有接口的请求/响应格式
- 直接在线测试接口
- 复制示例代码

#### 步骤 6：开发完成后的切换

当后端接口开发完成后，只需修改 API_BASE：

```javascript
// 切换回真实后端
const API_BASE = 'http://real-backend-server:8080/api'
```

**优势**：
- ✅ 不依赖后端开发进度
- ✅ 可以提前开始前端开发
- ✅ 接口变更时快速验证
- ✅ 离线也能开发

---

## 🧪 场景 2：测试开发 - 创建测试环境

### 真实案例：为自动化测试创建稳定的测试环境

#### 步骤 1：录制完整的用户流程

```bash
# 启动虚拟商店
python tests/gauntlet/dummy_shop.py

# 录制完整购物流程
python scripts/gauntlet_recorder.py
```

这会录制：
- 用户登录
- 浏览商品
- 添加到购物车
- 使用优惠券
- 下单支付
- 查看订单

#### 步骤 2：生成带安全保护的 Mock

```bash
# 生成 Mock（自动注入安全中间件）
mockclaw generate tests/gauntlet/flow.har --no-llm
```

生成的 Mock 服务器包含：
- ✅ 路径遍历保护
- ✅ 限流保护（60 请求/分钟）
- ✅ 安全错误处理

#### 步骤 3：在测试中使用

```python
# test_checkout.py
import requests

MOCK_BASE = 'http://localhost:8000'

def test_add_to_cart():
    """测试添加到购物车"""
    response = requests.post(
        f'{MOCK_BASE}/cart/user123',
        json={'product_id': 'p001', 'quantity': 2}
    )
    assert response.status_code == 200
    assert response.json()['status'] == 'success'

def test_checkout_with_coupon():
    """测试使用优惠券下单"""
    # 先添加商品到购物车
    requests.post(f'{MOCK_BASE}/cart/user123', 
                  json={'product_id': 'p001', 'quantity': 1})
    
    # 使用优惠券下单
    response = requests.post(
        f'{MOCK_BASE}/checkout',
        json={
            'user_id': 'user123',
            'coupon_code': 'DISCOUNT10'
        }
    )
    assert response.status_code == 200
    assert response.json()['status'] == 'success'

def test_expired_coupon():
    """测试过期优惠券"""
    response = requests.post(
        f'{MOCK_BASE}/checkout',
        json={
            'user_id': 'user123',
            'coupon_code': 'EXPIRED_COUPON'
        }
    )
    assert response.status_code == 400
    assert 'expired' in response.json()['message']
```

#### 步骤 4：运行测试

```bash
# 确保 Mock 服务器在运行
# mockclaw serve generated_mocks --port 8000

# 运行测试
pytest test_checkout.py -v
```

#### 步骤 5：运行混沌测试（可选）

```bash
# 测试 Mock 服务器的健壮性
mockclaw test generated_mocks
```

测试项目：
- 并发测试：50 个并行请求
- 垃圾数据：null、XSS、SQL 注入
- 路径遍历：`../`、`%2e%2e` 等
- 限流测试：100 个快速请求

**优势**：
- ✅ 测试环境完全可控
- ✅ 可以模拟各种边界情况
- ✅ 测试可重复、稳定
- ✅ 不依赖外部系统

---

## 🔌 场景 3：本地调试 - 模拟第三方 API

### 真实案例：调试调用微信支付 API 的代码

#### 步骤 1：录制真实 API 调用

1. 修改代码，启用日志：
```python
# 添加 HTTP 日志
import logging
logging.basicConfig(level=logging.DEBUG)
```

2. 执行一次真实的 API 调用：
```python
import requests

response = requests.post(
    'https://api.mch.weixin.qq.com/v3/pay/transactions/jsapi',
    headers={
        'Authorization': 'Bearer YOUR_TOKEN',
        'Content-Type': 'application/json'
    },
    json={
        'appid': 'wx123456',
        'amount': {'total': 100},
        'description': '测试商品'
    }
)
```

3. 保存请求/响应为 HAR 文件

#### 步骤 2：生成 Mock 服务器

```bash
mockclaw generate wechat_payment.har --no-llm
```

#### 步骤 3：修改 hosts 文件（Windows）

以管理员身份运行记事本，编辑：
```
C:\Windows\System32\drivers\etc\hosts
```

添加：
```
127.0.0.1 api.mch.weixin.qq.com
```

#### 步骤 4：修改 Mock 服务器端口

```bash
# 启动在 443 端口需要管理员权限，改用 8443
mockclaw serve generated_mocks --port 8443
```

#### 步骤 5：配置代理（可选）

或者使用代理方式（无需修改 hosts）：

```python
import requests

proxies = {
    'https': 'http://localhost:8443'
}

response = requests.post(
    'https://api.mch.weixin.qq.com/v3/pay/transactions/jsapi',
    proxies=proxies,
    verify=False  # 忽略 SSL 验证
)
```

#### 步骤 6：随意调试

现在你可以：
- ✅ 随意修改返回数据
- ✅ 模拟各种错误情况
- ✅ 测试超时处理
- ✅ 不触发真实 API 限流
- ✅ 不花一分钱！

**优势**：
- ✅ 调试成本极低
- ✅ 可以模拟各种异常情况
- ✅ 不触发 API 限流
- ✅ 节省测试费用

---

## ⚡ 常用命令速查表

### 启动服务

```bash
# 启动后端
python src/brain.py

# 启动前端
cd web && npm run dev

# 一键启动（Windows）
start.bat

# 使用 Docker
docker-compose up -d
```

### 录制流量

```bash
# 启动虚拟商店
python tests/gauntlet/dummy_shop.py

# 录制用户会话
python scripts/gauntlet_recorder.py
```

### 生成 Mock

```bash
# 使用命令行
mockclaw generate traffic.har

# 不使用 LLM（快速模式）
mockclaw generate traffic.har --no-llm

# 使用智能路由
mockclaw generate traffic.har --smart-fallback
```

### 启动 Mock 服务器

```bash
# 默认端口 8000
mockclaw serve generated_mocks

# 指定端口
mockclaw serve generated_mocks --port 8080

# 启用热重载
mockclaw serve generated_mocks --reload
```

### 测试

```bash
# 运行单元测试
pytest tests/

# 运行混沌测试
mockclaw test generated_mocks

# 运行强化混沌测试
mockclaw test generated_mocks --hardcore
```

### CLI 帮助

```bash
# 查看所有命令
mockclaw --help

# 查看具体命令帮助
mockclaw generate --help
mockclaw serve --help
```

---

## ❓ 常见问题解答

### Q1：没有 LLM API Key 能用吗？
**A**：可以！使用 `--no-llm` 参数即可：
```bash
mockclaw generate traffic.har --no-llm
```
这会使用智能路由模式，无需 AI 也能生成 Mock。

### Q2：HAR 文件是什么？
**A**：HAR（HTTP Archive）是浏览器导出的网络请求记录文件。
- 浏览器 F12 → Network 标签
- 右键 → "Save all as HAR with content"

### Q3：生成的 Mock 服务器性能如何？
**A**：基于 FastAPI，性能非常好：
- 单接口 QPS：1000+
- 并发连接：100+
- 响应时间：<10ms

### Q4：Mock 数据能自定义吗？
**A**：可以！生成的代码在 `generated_mocks/dynamic_api.py`，直接修改即可。

### Q5：支持哪些 HTTP 方法？
**A**：支持所有标准方法：
- GET、POST、PUT、DELETE、PATCH
- OPTIONS、HEAD 等

### Q6：如何处理动态参数？
**A**：MockClaw 支持路径参数：
```python
# /users/{user_id}/orders
@app.get("/users/{user_id}/orders")
async def get_user_orders(user_id: str):
    return {"user_id": user_id, "orders": [...]}
```

### Q7：能模拟错误响应吗？
**A**：可以！HAR 文件中包含的错误响应会被自动学习：
- 400 Bad Request
- 401 Unauthorized
- 404 Not Found
- 500 Internal Server Error

### Q8：如何更新 Mock 数据？
**A**：重新录制 HAR 文件并重新生成：
```bash
mockclaw generate new_traffic.har --no-llm
```

### Q9：支持文件上传吗？
**A**：支持！HAR 文件中的 multipart/form-data 会被自动处理。

### Q10：如何在团队中共享 Mock？
**A**：三种方式：
1. 提交 `generated_mocks/` 到 Git
2. 使用 Docker 部署：
   ```bash
   docker-compose up -d
   ```
3. 部署到服务器：
   ```bash
   uvicorn generated_mocks.dynamic_api:app --host 0.0.0.0 --port 80
   ```

---

## 🎓 进阶技巧

### 技巧 1：组合多个 HAR 文件

```bash
# 合并多个 HAR 文件
python scripts/merge_har.py file1.har file2.har -o merged.har

# 生成 Mock
mockclaw generate merged.har --no-llm
```

### 技巧 2：自定义响应延迟

编辑 `generated_mocks/dynamic_api.py`：
```python
import asyncio

@app.get("/products")
async def get_products():
    await asyncio.sleep(0.5)  # 模拟 500ms 延迟
    return {...}
```

### 技巧 3：添加自定义逻辑

```python
@app.post("/checkout")
async def checkout(data: CheckoutRequest):
    # 自定义验证逻辑
    if data.amount > 10000:
        raise HTTPException(400, "金额过大")
    
    # 调用真实支付接口
    # ...
    
    return {"status": "success"}
```

### 技巧 4：使用中间件

```python
# 添加认证中间件
@app.middleware("http")
async def auth_middleware(request, call_next):
    token = request.headers.get("Authorization")
    if not token:
        return JSONResponse({"error": "Unauthorized"}, status_code=401)
    
    response = await call_next(request)
    return response
```

### 技巧 5：集成到 CI/CD

```yaml
# .github/workflows/test.yml
name: Tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Setup Mock Server
        run: |
          pip install -r src/requirements.txt
          mockclaw generate tests/traffic.har --no-llm
          mockclaw serve generated_mocks --port 8000 &
      
      - name: Run Tests
        run: pytest tests/
```

---

## 📖 学习资源

### 官方文档
- README.md：项目概述
- CHANGELOG.md：更新日志
- docs/architecture.md：架构说明

### 示例代码
- `tests/gauntlet/`：Gauntlet 测试示例
- `generated_mocks/`：生成的 Mock 代码
- `scripts/`：各种工具脚本

### 社区资源
- GitHub Issues：问题反馈
- FastAPI 文档：https://fastapi.tiangolo.com
- HAR 格式规范：https://w3c.github.io/web-performance/specifications/HAR/

---

## 🎉 总结

MockClaw 是你日常开发的好帮手：

### 适合人群
- ✅ 前端开发：不再依赖后端接口
- ✅ 测试开发：创建稳定测试环境
- ✅ 后端开发：快速原型验证
- ✅ 全栈开发：提高开发效率

### 核心优势
- ✅ **快速**：5 分钟生成完整 Mock 服务器
- ✅ **简单**：拖放 HAR 文件即可
- ✅ **智能**：AI 辅助生成（可选）
- ✅ **健壮**：自动注入安全保护
- ✅ **灵活**：支持自定义和扩展

### 开始使用
```bash
# 1. 启动服务
start.bat

# 2. 录制流量
python scripts/gauntlet_recorder.py

# 3. 生成 Mock
mockclaw generate tests/gauntlet/flow.har --no-llm

# 4. 启动服务器
mockclaw serve generated_mocks --port 8000

# 5. 访问文档
# http://localhost:8000/docs
```

**祝你开发愉快！** 🚀

---

*最后更新：2026-03-29*
*版本：v0.2.0*
